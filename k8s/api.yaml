apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hono-api
  template:
    metadata:
      labels:
        app: hono-api
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: api-container
          image: ghcr.io/andrzey/my-api:f769516bc42a42ca85f44408c60edba73201ca98
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          env:
            - name: DATABASE_URL
              value: "postgres://myuser:mypassword@host.k3d.internal:5432/mydb"
            - name: BETTER_AUTH_URL
              value: "http://localhost:8080/api"
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: hono-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
