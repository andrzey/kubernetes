apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hono-api
  template:
    metadata:
      labels:
        app: hono-api
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: api-container
          image: ghcr.io/andrzey/my-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_URL
              value: "postgres://myuser:mypassword@host.k3d.internal:5432/mydb"
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: hono-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
